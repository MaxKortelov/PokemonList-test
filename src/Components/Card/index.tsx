import React, {FC} from 'react';

import styles from './Card.module.sass';

const Card : FC = () => {
    return(
        <div className={styles.cardListWrap}>
            123
        </div>
    )
}

export default Card;